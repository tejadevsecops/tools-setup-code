input {
  beats {
    port => 5044
  }
}
output {
  elasticsearch {
    hosts => ["https://localhost:9200"]
    index => "test"
    user => "elastic"
    password => "*-BZ_r7neW9*Sh-x2=Lz"
    ssl_verification_mode => "none"
  }
}
